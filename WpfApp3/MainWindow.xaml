<Window x:Class="WpfApp3.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Фабричный метод - Лабораторная работа №3" 
        Height="333" 
        Width="394"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBlock Text="Выберите цвет фигур:" 
                       VerticalAlignment="Center" 
                       FontSize="14"
                       Margin="0,0,15,0"/>

            <ComboBox x:Name="ColorComboBox" 
                      Width="150" 
                      Height="30"
                      FontSize="14"
                      SelectionChanged="ColorComboBox_SelectionChanged">
                <ComboBoxItem Content="Красный" IsSelected="True"/>
                <ComboBoxItem Content="Синий"/>
                <ComboBoxItem Content="Зелёный"/>
            </ComboBox>
        </StackPanel>

        <Border Grid.Row="1" 
                BorderBrush="LightGray" 
                BorderThickness="1" 
                CornerRadius="5"
                Padding="10"
                Background="#F9F9F9">
            <ScrollViewer VerticalScrollBarVisibility="Auto" 
                          HorizontalScrollBarVisibility="Auto">
                <WrapPanel x:Name="FiguresPanel" 
                           Orientation="Horizontal"
                           HorizontalAlignment="Left"/>
            </ScrollViewer>
        </Border>

        <Button Grid.Row="2" 
                Content="Создать набор фигур" 
                Click="AddFiguresButton_Click" 
                Margin="0,15,0,0" 
                Height="40"
                FontSize="14"
                Foreground="Black"
                BorderThickness="0"
                Cursor="Hand"/>
    </Grid>
</Window>